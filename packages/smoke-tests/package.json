{
  "name": "@social-media-app/smoke-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "Smoke tests for deployment validation",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vitest --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@social-media-app/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}