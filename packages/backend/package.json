{
  "name": "@social-media-app/backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Lambda functions for social media app",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.0.0",
    "@aws-lambda-powertools/metrics": "^2.0.0",
    "@aws-lambda-powertools/tracer": "^2.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@social-media-app/dal": "workspace:*",
    "@social-media-app/shared": "workspace:*",
    "jose": "^5.0.0",
    "lodash-es": "^4.17.21",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/lodash-es": "^4.17.0",
    "@types/node": "^22.0.0",
    "aws-lambda": "^1.0.7",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}