# =============================================================================
# LOCAL DEVELOPER OVERRIDES - Copy to .env.local (GITIGNORED)
# =============================================================================
# Copy this file to .env.local and customize for your local environment.
# .env.local is gitignored and should contain your personal settings and secrets.

# JWT Secrets (REQUIRED - Generate your own secure secrets)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-local-jwt-secret-minimum-32-characters-here-replace-with-secure-value
JWT_REFRESH_SECRET=your-local-jwt-refresh-secret-minimum-32-characters-here-replace-with-secure-value

# Optional: Override log level for your local development
# LOG_LEVEL=trace

# Optional: Override table name for your local DynamoDB
# TABLE_NAME=my-custom-local-table

# Optional: Enable console logs for Next.js
# CONSOLE_LOGS=true

# Optional: Override service name
# OTEL_SERVICE_NAME=my-custom-service-name
